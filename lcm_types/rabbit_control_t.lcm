package lcm_msgs;

struct rabbit_control_t
{
    int64_t timestamp;

    double qj_tau[4];

    double qj_pos[4];
    double qj_vel[4];

    double kp[4];
    double kd[4];

    double se_pos[3]; // Estimated position
    double se_vel[3]; // Estimated velocity
    double EA[3]; // Euler Angles
}
